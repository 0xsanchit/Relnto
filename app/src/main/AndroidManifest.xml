<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.relnto">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/logof"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".ViewOrdersActivity"></activity>
        <activity android:name=".Final_Activity" />
        <activity android:name=".SearchProductsActivity" />
        <activity android:name=".ConfirmFinalOrderActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".RentHomeActivity"
            android:label="@string/title_activity_rent_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddProductForLending" />
        <activity android:name=".LendHomeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ActivityLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>